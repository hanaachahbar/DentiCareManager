// Only needed if your app stores data locally, like:

// Patient profiles 🧍

// Appointments 📅

// Payments 💰
// It uses SQLite, which works perfectly offline — ideal for your dentist management system.

// If you’re just doing the UI in Sprint 1, you can skip it for now.
// When you move to Sprint 2 (functionality), you’ll add it to save and load data.